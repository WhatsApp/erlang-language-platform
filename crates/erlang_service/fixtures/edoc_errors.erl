%% @doc This module contains a backtick,
%%      which causes `EDoc to crash and burn
-module(edoc_errors).

%% @doc This is function 1
one() ->
    1.

%% @docc This is function two, containing a warning
two() ->
    2.
