error: reveal_type (See https://fb.me/eqwalizer_errors#reveal_type)
   ┌─ check/src/dynamic_constraints.erl:19:25
   │
19 │   eqwalizer:reveal_type(cons(ok, L)).
   │                         ^^^^^^^^^^^ ['ok' | dynamic()]

error: reveal_type (See https://fb.me/eqwalizer_errors#reveal_type)
   ┌─ check/src/dynamic_constraints.erl:32:25
   │
32 │   eqwalizer:reveal_type(expect(E)).
   │                         ^^^^^^^^^ atom()

error: reveal_type (See https://fb.me/eqwalizer_errors#reveal_type)
   ┌─ check/src/dynamic_constraints.erl:53:25
   │
53 │   eqwalizer:reveal_type(set(b, 3, KVs)).
   │                         ^^^^^^^^^^^^^^ kvs('b' | dynamic(), dynamic() | integer())

error: reveal_type (See https://fb.me/eqwalizer_errors#reveal_type)
   ┌─ check/src/dynamic_constraints.erl:60:5
   │
60 │     maps:from_list(set(b, 3, KVs))
   │     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ #{'b' | dynamic() => dynamic() | integer()}

error: incompatible_types (See https://fb.me/eqwalizer_errors#incompatible_types)
   ┌─ check/src/dynamic_constraints.erl:67:3
   │    
67 │ ╭ ╭   maps:merge(
68 │ │ │     #{a => 1, b => 2},
69 │ │ │     maps:from_list(set(b, 3, KVs))
70 │ │ │   ).
   │ ╰─│───^ maps:merge(#{..}, maps:from_list(set('b', 3, KVs))).
Expression has type:   #{a := integer(), b := integer(), 'b' | dynamic() => dynamic() | integer()}
Context expected type: #{a := number(), b := number()}
   │   ╰───' 

Because in the expression's type:
  Here the type is:     #{'b' | dynamic() => dynamic() | integer()}
  Context expects type: #{...} (no default association)
  The expected map has no default association while the type of the expression has one.

5 ERRORS
