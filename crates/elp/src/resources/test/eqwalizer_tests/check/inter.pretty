error: incompatible_types (See https://fb.me/eqwalizer_errors#incompatible_types)
   ┌─ check/src/inter.erl:22:22
   │
22 │ map_inter1_neg1() -> #{a => a}.
   │                      ^^^^^^^^^
   │                      │
   │                      #{..}.
Expression has type:   #{a := 'a'}
Context expected type: i(ma1(), mb1())
   │                      

Because in the expression's type:
  Here the type is:     #{a := 'a'}
  Context expects type: ma1() & mb1()

error: incompatible_types (See https://fb.me/eqwalizer_errors#incompatible_types)
   ┌─ check/src/inter.erl:28:22
   │
28 │ map_inter2_neg1() -> #{a => ~""}.
   │                      ^^^^^^^^^^^
   │                      │
   │                      #{..}.
Expression has type:   #{a := binary()}
Context expected type: i(ma2(), mb2())
   │                      

Because in the expression's type:
  Here the type is:     #{a := binary()}
  Context expects type: ma2() & mb2()

error: incompatible_types (See https://fb.me/eqwalizer_errors#incompatible_types)
   ┌─ check/src/inter.erl:44:22
   │
44 │ b_id_app_neg1(AB) -> b_id(AB).
   │                      ^^^^^^^^
   │                      │
   │                      b_id(AB).
Expression has type:   i(atom() | binary(), atom() | binary())
Context expected type: atom()
   │                      

Because in the expression's type:
  Here the type is a union type with some valid candidates: atom()
  However the following candidate: binary()
  Differs from the expected type:  atom()

3 ERRORS
