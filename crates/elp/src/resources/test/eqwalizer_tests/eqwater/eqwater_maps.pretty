error: incompatible_types (See https://fb.me/eqwalizer_errors#incompatible_types)
   ┌─ eqwater/src/eqwater_maps.erl:26:53
   │
26 │ map_occ_04_neg(M = #{a := I}) when is_integer(I) -> M;
   │                                                     ^
   │                                                     │
   │                                                     M.
Expression has type:   #{a := integer(), b => integer()}
Context expected type: #{a := integer(), b := integer()}
   │                                                     

Because in the expression's type:
  Here the type is:     #{b => ..., ...}
  Context expects type: #{b := ..., ...}
  The type of the expression is missing the following required keys: b.

error: incompatible_types (See https://fb.me/eqwalizer_errors#incompatible_types)
   ┌─ eqwater/src/eqwater_maps.erl:27:22
   │
27 │ map_occ_04_neg(M) -> M#{a => 0}.
   │                      ^^^^^^^^^^
   │                      │
   │                      ..#{..}.
Expression has type:   #{a := integer(), b => integer()}
Context expected type: #{a := integer(), b := integer()}
   │                      

Because in the expression's type:
  Here the type is:     #{b => ..., ...}
  Context expects type: #{b := ..., ...}
  The type of the expression is missing the following required keys: b.

error: incompatible_types (See https://fb.me/eqwalizer_errors#incompatible_types)
   ┌─ eqwater/src/eqwater_maps.erl:36:22
   │
36 │ map_occ_06_neg(V) -> V.
   │                      ^
   │                      │
   │                      V.
Expression has type:   #{} | 'ok'
Context expected type: 'ok'
   │                      

Because in the expression's type:
  Here the type is a union type with some valid candidates: 'ok'
  However the following candidate: #{}
  Differs from the expected type:  'ok'

error: incompatible_types (See https://fb.me/eqwalizer_errors#incompatible_types)
   ┌─ eqwater/src/eqwater_maps.erl:42:22
   │
42 │ map_occ_07_neg(M) -> M.
   │                      ^
   │                      │
   │                      M.
Expression has type:   #{c := integer()}
Context expected type: #{b := integer()}
   │                      

Because in the expression's type:
  Here the type is:     #{...}
  Context expects type: #{b := ..., ...}
  The type of the expression is missing the following required keys: b.

error: incompatible_types (See https://fb.me/eqwalizer_errors#incompatible_types)
   ┌─ eqwater/src/eqwater_maps.erl:55:34
   │
55 │ map_occ_08_neg(#{a := _} = M) -> M;
   │                                  ^ M.
Expression has type:   #{a := dynamic(), dynamic() => dynamic()}
Context expected type: 'err'

error: incompatible_types (See https://fb.me/eqwalizer_errors#incompatible_types)
   ┌─ eqwater/src/eqwater_maps.erl:70:29
   │
70 │     (_, #{a := V}) -> is_ok(V)
   │                             ^
   │                             │
   │                             V.
Expression has type:   'err' | 'ok'
Context expected type: 'ok'
   │                             

Because in the expression's type:
  Here the type is a union type with some valid candidates: 'ok'
  However the following candidate: 'err'
  Differs from the expected type:  'ok'

error: incompatible_types (See https://fb.me/eqwalizer_errors#incompatible_types)
   ┌─ eqwater/src/eqwater_maps.erl:85:39
   │
85 │     #{K := V} when V =/= undefined -> V;
   │                                       ^
   │                                       │
   │                                       V.
Expression has type:   'undefined' | integer()
Context expected type: integer()
   │                                       

Because in the expression's type:
  Here the type is a union type with some valid candidates: integer()
  However the following candidate: 'undefined'
  Differs from the expected type:  integer()

error: incompatible_types (See https://fb.me/eqwalizer_errors#incompatible_types)
    ┌─ eqwater/src/eqwater_maps.erl:100:58
    │
100 │     #{K := #frag{index = Idx}} when Idx =/= undefined -> Idx;
    │                                                          ^^^
    │                                                          │
    │                                                          Idx.
Expression has type:   'undefined' | integer()
Context expected type: integer()
    │                                                          

Because in the expression's type:
  Here the type is a union type with some valid candidates: integer()
  However the following candidate: 'undefined'
  Differs from the expected type:  integer()

error: incompatible_types (See https://fb.me/eqwalizer_errors#incompatible_types)
    ┌─ eqwater/src/eqwater_maps.erl:108:65
    │
108 │     #{K := #frag{index = Idx} = Frag} when Idx =/= undefined -> {Idx, Frag};
    │                                                                 ^^^^^^^^^^^
    │                                                                 │
    │                                                                 {Idx, Frag}.
Expression has type:   {'undefined' | integer(), #frag{}}
Context expected type: {integer(), #frag{}}
    │                                                                 

Because in the expression's type:
  { 
    Here the type is a union type with some valid candidates: integer()
    However the following candidate: 'undefined'
    Differs from the expected type:  integer()
  , #frag{}}

error: incompatible_types (See https://fb.me/eqwalizer_errors#incompatible_types)
    ┌─ eqwater/src/eqwater_maps.erl:117:56
    │
117 │     #{K := T} when T =/= false, T =/= indeterminate -> T;
    │                                                        ^
    │                                                        │
    │                                                        T.
Expression has type:   'false' | 'indeterminate' | replica()
Context expected type: replica()
    │                                                        

Because in the expression's type:
  Here the type is a union type with some valid candidates: 'full' | 'witness'
  However the following candidate: 'false'
  Differs from the expected type:  'full' | 'witness'

error: incompatible_types (See https://fb.me/eqwalizer_errors#incompatible_types)
    ┌─ eqwater/src/eqwater_maps.erl:125:3
    │    
125 │ ╭ ╭   maps:fold(
126 │ │ │     fun(K, _, Acc) ->
127 │ │ │       case M of
128 │ │ │         #{K := V} when V =/= undefined -> [V | Acc];
    · │ │
133 │ │ │     Keys
134 │ │ │   ).
    │ ╰─│───^ maps:fold(fun, [], Keys).
Expression has type:   ['undefined' | integer()]
Context expected type: [integer()]
    │   ╰───' 

Because in the expression's type:
  [
    Here the type is a union type with some valid candidates: integer()
    However the following candidate: 'undefined'
    Differs from the expected type:  integer()
  ]

error: incompatible_types (See https://fb.me/eqwalizer_errors#incompatible_types)
    ┌─ eqwater/src/eqwater_maps.erl:140:41
    │
140 │     (#{K := V}) when V =/= undefined -> V;
    │                                         ^
    │                                         │
    │                                         V.
Expression has type:   'undefined' | integer()
Context expected type: integer()
    │                                         

Because in the expression's type:
  Here the type is a union type with some valid candidates: integer()
  However the following candidate: 'undefined'
  Differs from the expected type:  integer()

error: incompatible_types (See https://fb.me/eqwalizer_errors#incompatible_types)
    ┌─ eqwater/src/eqwater_maps.erl:149:7
    │
149 │       #{{K, Idx} => Frag};
    │       ^^^^^^^^^^^^^^^^^^^
    │       │
    │       #{..}.
Expression has type:   #{{atom(), 'undefined' | integer()} => #frag{}}
Context expected type: #{{atom(), integer()} => #frag{}}
    │       

Because in the expression's type:
  #{ map domains are incompatible:
    { atom(), 
      Here the type is a union type with some valid candidates: integer()
      However the following candidate: 'undefined'
      Differs from the expected type:  integer()
    }
  , ... }

error: incompatible_types (See https://fb.me/eqwalizer_errors#incompatible_types)
    ┌─ eqwater/src/eqwater_maps.erl:158:50
    │
158 │     #{K := P} when P =/= undefined -> element(1, P);
    │                                                  ^
    │                                                  │
    │                                                  P.
Expression has type:   'undefined' | {atom(), integer()}
Context expected type: tuple()
    │                                                  

Because in the expression's type:
  Here the type is a union type with some valid candidates: {atom(), integer()}
  However the following candidate: 'undefined'
  Differs from the expected type:  tuple()

error: incompatible_types (See https://fb.me/eqwalizer_errors#incompatible_types)
    ┌─ eqwater/src/eqwater_maps.erl:166:52
    │
166 │     #{K := #job{part = P}} when P =/= undefined -> P;
    │                                                    ^
    │                                                    │
    │                                                    P.
Expression has type:   'undefined' | binary()
Context expected type: binary()
    │                                                    

Because in the expression's type:
  Here the type is a union type with some valid candidates: binary()
  However the following candidate: 'undefined'
  Differs from the expected type:  binary()

15 ERRORS
